# Config (contains secrets)
config.json
config.env
*.env
!config.example.json
!config.env.example

# Build artifacts
dist/
build/
*.exe
*.apk
*.idsig
*.spec
*.egg-info/

# Android build artifacts + signing
android/*/bin/
android/*/gen/
android/*/obj/
android/*/classes/
android/*/*.apk
android/*/*.apk.idsig
android/*/classes.zip
*.keystore
debug.keystore

# JVM conformance / unit-test build output (android/build-conformance.sh)
build-conformance/

# Keys and pairing
keys/
pairing-codes/
lion_pubkey.pem
*.pem

# IDE / editor
.claude/
__pycache__/
*.pyc
.vscode/
.idea/
*.swp
*~

# Test + coverage artifacts
.coverage
.pytest_cache/
htmlcov/
.tox/

# OS
.DS_Store
Thumbs.db

# Personal / deployment
*.ab
trusted_peers.json
orders.json
peers.json

# Session-specific coordination docs (not for distribution)
SESSION-HANDOFF-*.md
RESTART-PROMPT.md
ANNOUNCE-DRAFT.md
docs/ACCOUNT-MESH-DESIGN.md

# Backups + enforcement state (personal, not for distribution)
*.bak
*.bak-*
*.bak.*
enforcement-memory/
memory/

# Personal UX feedback artifact — blank template with an unanswered
# "your_answer" column, not meant for public tracking.
/lions-share-ux-questionnaire.csv

# Staging QA harness runtime state
staging/.relay.pid
staging/.relay.log
staging/qa-matrix-result.json

# Per-operator mesh installers (embed specific relay URLs + mesh IDs that
# PR #46 scrubbed from the public repo — keep them local). The generic
# install-mesh.sh wrapper is whitelisted back in.
installers/install-mesh-*.sh
!installers/install-mesh.sh

# Local archive of superseded builds / session docs (tidy-up, not for distribution)
archive/
