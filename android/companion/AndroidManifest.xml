<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bunnytasker"
    android:versionCode="59"
    android:versionName="2.26">

    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="36" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Package visibility (Android 11+). The banking-app picker probes each
         candidate with getLaunchIntentForPackage(); that returns null for any
         package we can't "see". QUERY_ALL_PACKAGES alone is fragile (Play-
         restricted, and can be denied), so also declare visibility of every app
         that has a launcher activity — this is what lets installed banks like
         Wise / PayPal / Tangerine actually appear in the picker. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:label="Bunny Tasker"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/BunnyTheme"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- First-run warm welcome (started by MainActivity for result).
             Standard launch mode so it returns its result to the singleTask
             MainActivity. Independent of the Collar's Terms of Surrender. -->
        <activity
            android:name=".BunnyWelcomeActivity"
            android:exported="false"
            android:theme="@style/BunnyTheme"
            android:screenOrientation="portrait" />

        <service
            android:name=".BunnyService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <receiver
            android:name=".AdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
