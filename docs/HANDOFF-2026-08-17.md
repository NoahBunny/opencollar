# Handoff — 2026-08-17 (loose ends: harness honesty, a lying flag, merge prep)

Follows `HANDOFF-2026-08-08.md`. The two sessions in between (2026-08-16/17)
landed the live-mesh and deploy-path work recorded in the CHANGELOG but never
got a handoff doc; this one covers the tidy-up pass and states plainly where
`feat/real-mesh-bunnies` stands relative to `main`.

Theme of everything below: **the system was behaving correctly and reporting
itself incorrectly.** Four separate places where the record disagreed with the
thing enforcing it. None was a live compromise. All four are the same failure
mode that cost hours on the deploy path this week.

## What landed

1. **A green test run was citing the other checkout's files** (`71c70eb`).
   This repo is mirrored into `~/Nextcloud` — a different repo on a different
   branch — and the sync carries `__pycache__` with it. A `.pyc` records the
   compiling checkout's absolute path in `co_filename`, and Python revalidates
   it against the local source's mtime+size only, which a file-level sync
   preserves. So pytest collected our files and reported every skip location
   and traceback against the mirror. Audited before touching anything: **99 of
   130** in-tree `.pyc` carried the mirror's path, 47 of them live. **None was
   executing different code** — the sources matched byte for byte. Purged;
   `PYTHONPYCACHEPREFIX` now puts bytecode outside the tree so the sync has
   nothing to carry; `tests/conftest.py` aborts the session if any collected
   test's `co_filename` resolves outside the repo root
   (`FOCUSLOCK_ALLOW_FOREIGN_PYC=1` downgrades to a warning). Verified by
   planting a foreign assertion-rewritten `.pyc` the way the sync would.

2. **The auto-accept flag claimed a door the gate holds shut** (`30dbd96`).
   `_auto_accept_active()` fails closed on an expired or missing deadline, so
   no mesh was accepting anyone — but nothing wrote that verdict back, and
   `auto_accept_nodes` sat `true` in the account JSON forever. That file is what
   an operator reads on the relay to answer "is the door open?", and on
   `Mn4pQr7tVw2X` it had been answering *yes*. Both HTTP readouts were already
   honest, so this was never a hole. New `_close_expired_auto_accept_windows()`
   reconciles the flag at every relay start. **An open window is never
   shortened.** 5 tests.

3. **The relay's run dir was world-writable, past an audit gate** (`7e13dc5`).
   `homelab-install.sh` created `/run/focuslock` **0777** plus a tmpfiles.d rule
   to re-apply it every boot. Both relay units run as root (no `User=`), so the
   write bit buys nothing — it was inherited from the desktop-collar installer,
   where it is correct. What it cost: `/webhook/controller-register` is
   admin-token-gated (audit 2026-04-27 M-2) precisely so an unauth caller cannot
   point controller resolution at an address of their choosing, and a
   world-writable directory lets any local account on the relay write
   `controller.json` straight past that gate. Now 0755 in both the chmod and the
   tmpfiles rule. 4 regression tests, mutation-checked.

4. **The lint gate this branch had drifted past** (`36d02b0`). `main` is clean
   under both ruff gates; this branch had accumulated 5 check errors and 16
   unformatted files across 86 commits, so CI would have failed the merge. The
   reason it went unnoticed: **`make lint` was itself broken** — it invoked a
   bare `ruff`, absent on a machine carrying ruff only in `.venv`, so the target
   died instead of linting. Same off-PATH gate failure as `b4010e9`. Also bound
   a loop variable in `staging/qa_device_ui.py` that tripped B023 (latent, not
   live — every call happens inside its own iteration).

## Verification

- `make lint` → clean (`ruff check` + `ruff format --check`, 123 files).
- `mypy shared` → no issues, 11 source files.
- `make qa-pytest` → **1299 passed, 21 skipped**.
- `make qa-cov-mesh` → gated mesh coverage floor holds.
- `bash android/build-conformance.sh` → **80 JVM tests, 0 failed**.
- `bash scripts/sign-branch.sh` → all 73 sensitive commits signed, 0 unsigned.
- `tests/test_android_conformance.py` with both conformance CLIs exported →
  **30 passed** (17 of those normally skip without the CLIs).
- All three APKs build and `apksigner verify` clean — Collar 240 KB, Bunny
  Tasker 304 KB, Lion's Share 236 KB (default **Tor-OFF** builds, per the
  standing guidance to keep Tor default-off until this branch merges). Build
  artifacts are untracked; the working tree is clean.

## Merge status — `feat/real-mesh-bunnies` → `main`

`main` is 0 commits ahead; the branch is **91 ahead** and the merge base *is*
main's tip, so this is a fast-forward with no conflicts. **Every CI job now
verifies clean locally, signed-commits included.**

`signed-commits.yml` was the last blocker, and clearing it exposed that the gate
was **unsatisfiable** — signing alone would not have turned it green:

- Nothing set `gpg.ssh.allowedSignersFile`, so a runner **cannot verify an SSH
  signature at all**: `%G?` reads `N`, the identical status an unsigned commit
  reports. A contributor who followed CONTRIBUTING.md exactly would have signed
  every commit and still watched CI fail, with an error telling them to sign.
- CONTRIBUTING's retroactive one-liner signs nothing unless signing is already
  configured, and this machine had no `commit.gpgsign` / `user.signingkey` /
  `gpg.format`. The rebase would have rewritten the branch and changed no
  signature status.

Fixed in `2a05c77`: `.github/allowed_signers` holds the trusted keys, the
workflow points git at it before checking, and that file is itself in
`SENSITIVE_REGEX` — a trust list an unsigned commit can append to is not a trust
list. `scripts/sign-branch.sh` does the rest (report by default, `--apply` to
rewrite; refuses a dirty tree, merge commits, or detached HEAD; never mints a
key; backs up first; re-verifies with the workflow's regex copied byte-for-byte;
never pushes).

Run on this branch 2026-08-17: **all 91 commits verify `G`** — not merely the 73
touching sensitive paths — and the tree is byte-identical to
`backup/pre-signing/feat/real-mesh-bunnies-20260817-023153`, so the rewrite added
signatures and changed nothing else. Confirmed by simulating the runner: fresh
clone, no local config, the workflow's own logic → *"OK — all commits touching
sensitive paths are signed."*

**The branch has not been force-pushed.** Every SHA changed, so `origin` still
carries the 66 old unsigned commits and the push is deliberate:
`git push --force-with-lease origin feat/real-mesh-bunnies`. Two things the
script cannot do: the public half of the signing key must be registered on
GitHub as a **signing** key (an auth key of the same bytes does not count) or
commits read "Unverified" in the web UI, and GPG-signed commits still fail CI
because nothing imports a keyring into the runner — which is why CONTRIBUTING
now points at SSH.

## Still owed

**Closed 2026-08-17 (`b19dd26`) — `GET /controller` no longer hands out the
Lion's address.** The write side had been admin-gated since M-2 on the reasoning
that an unauth caller must not choose the address controller resolution returns;
the read side, which returns it, was open, on a relay reachable over public
HTTPS. Now gated with the same shape as `/standing-orders` (503 unconfigured,
403 on missing or wrong token) and failing closed rather than falling back to
public. Dispatch had to stop comparing the raw path — an authed call carries a
query string, and the old comparison would have 404'd every one. Its one caller,
`scripts/release.sh`, sends `FOCUSLOCK_ADMIN_TOKEN` as a **Bearer header** (a
query string lands in the access log, and this token is the whole admin API);
unset, it says so and falls back to `LION_DEVICE_IP` as before.

**Needs interactive sudo on the relay.** The running box is still 0777; this
session's ssh has no passwordless sudo. `sudo chmod 755 /run/focuslock`, then
re-run the installer so `/etc/tmpfiles.d/focuslock.conf` is rewritten — the
chmod alone reverts on the next boot.

**Needs a Lion tap, per node.** Trust provenance on `Mn4pQr7tVw2X` is thin.
Every node but `relay`/`controller` is stamped `confirmed_by: grandfathered` —
swept in by the one-shot migration, never actually looked at — and `gengar-neon`
was admitted from the relay console (`admitted_by: operator-console`) at the
bunny's request, without a Lion signature. The sweep was right for working
meshes, but a stamp that says *nobody checked* is not authority. One tap each in
Lion's Share → Vault Nodes replaces the inherited stamps with deliberate ones.

**Needs a relay deploy.** Fix 2 only takes effect when the relay restarts on the
new code. Not deployed from this session.

**Unchanged from 2026-08-08.** Release Forever teardown on A16/17 is
code-complete but never verified with device admin actually enabled; photo task
A-5 (camera + Ollama); the Windows collar and its `.exe` rebuild; `#6` re-lock
on tamper at SEALED (design idea, not started); and whether Release Forever
should set the terminal `released` flag the way the safeword path does.

## Notes for whoever picks this up

- `/run/focuslock` on the relay is now **vestigial for state** — `_STATE_DIR`
  defaults to `/var/lib/focuslock` and the unit sets it explicitly. But five
  call sites still hardcode `/run/focuslock/{devices,controller}.json`, which is
  tmpfs and does not survive a reboot. Lower stakes than the 2026-08-15 mesh
  wipe, same shape.
- The Android toolchain **was** available this session (`aapt2`/`d8`/`apksigner`/
  `zipalign`/`javac` all on PATH, `ANDROID_SDK=/home/<user>/android-sdk`), contrary
  to the standing note that it is not persistent on this box. Check before
  assuming either way.
