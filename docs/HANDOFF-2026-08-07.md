# Handoff — 2026-08-07 (ecosystem-review fix-forward: landed + committed)

Branch `feat/ecosystem-review-2026-05-25`. The previously-uncommitted feature
stack (cage tiers, optimistic order reflection, bunny display name, desktop-task
webhook, Windows liberation, panic `safeword.exe`) was reviewed with a
14-agent adversarial workflow (48 findings), fixed **full fix-forward**, and
**committed** as 5 logical commits on top of `c2625b3`:

```
b4f9ac3 chore(release): sync installer version targets + CHANGELOG + design-priorities
2ac3bf9 fix(desktop)+feat(tools): honest liberation, atomic standing-orders, safeword.exe, tamper ratchet
7c23438 fix(server)+feat(companion): auth-gate node PII, guard desktop-task, editable display name
570d67a fix(controller): E2EE key-substitution + optimistic-reflection correctness
ed1c717 fix(collar): restore safety-floor exits + author-backed cage consent
```

Full detail: `CHANGELOG.md` `[Unreleased]`. Versions: slave **80 / 8.37**,
controller **73 / 73.0**, companion **59 / 2.26**; `installers/re-enslave-lib.sh`
targets synced (were stale at 70/67/51).

## Verification status

**Automated — GREEN (all runnable layers, this workstation):**
- Python suite: **1177 passed, 18 skipped** (`.venv/bin/python -m pytest tests/`).
- Android conformance: **20 Java↔Python + 64 JVM unit** (`make qa-android`).
- All 3 APKs build + dex + sign + `apksigner verify`.
- `ruff check` clean on all changed Python.

**On-device — NOT DONE (needs a device / Windows box — the handoff below).**
Waydroid is uninitialized here and the SM-S908 was not attached; several checks
need a real phone (accessibility-service + real Settings/camera/telephony) or a
Windows machine (liberation). This is the part that matters most for this change
set: the riskiest fixes are UI/accessibility behaviors with no unit coverage.

## Device-QA checklist (keyed to the fixes)

Recage the SM-S908 with the new APKs (`installers/re-enslave-phones.sh`; slave 80
needs a fresh install to pick up the ShadeGuardService changes), then:

### Collar cage + safety floor (commit ed1c717) — the priority
1. **Consent chooser**: fresh install → Terms of Surrender shows the Leash/Collar/
   Sealed chooser; pick **Collar**. Confirm the ceiling is stored app-private
   (survives an ADB `settings put global focus_lock_cage_level 2` — the bridge
   write must NOT tighten past the chosen ceiling).
2. **Lock at COLLAR**: opening a non-allowlisted app (Maps/browser) bounces back to
   the jail; **Bunny Tasker, the banking app, the keyboard, the dialer** stay usable.
3. **Escape counter**: three bounced app-switches → `focus_lock_escapes` reaches 3
   → the in-app **factory-reset button appears**, and each bounce posts a server
   `escape` event (paywall tier advances).
4. **Settings reachability (floor)**: with escapes ≥ 3 (or after the Lion's
   `/api/enable-settings`), the Settings app is **reachable** — walk to
   Settings → factory reset without being bounced.
5. **Photo Task**: set mode=photo_task → the camera opens and a photo can be
   **taken + submitted** (not bounced) → task clears.
6. **SEALED**: normal dialer blocked, but an **emergency call** still places; an
   in-progress call is not bounced.
7. **Safeword still terminal**: trigger the safeword during a COLLAR lock → device
   releases and the watchdog does **not** re-jail (confirm `focus_lock_released=1`
   is honored by ShadeGuardService).
8. **Watchdog-off tamper**: disable the accessibility service mid-lock → the Lion
   gets a `shadeguard_disabled` event.

### Lion's Share (commit 570d67a)
9. Issue a lock/timer/balance order → the status bar/balance hold the commanded
   state (no snap-back), and a re-lock to extend the timer shows the **new** time.
10. Multi-bunny: switch bunnies mid-order → no leak of the previous bunny's
    lock/timer/balance or Collar addresses.
11. E2EE: after a mesh-invite pairing, the "not encrypted" banner clears, and a
    relay that advertises a *different* bunny key does **not** replace the verified
    one (check logcat for the "re-pair to rotate" warning).

### Companion + server (commit 7c23438)
12. **Display name**: long-press the paired fingerprint → edit → the Lion's node
    list shows the new name (via signed `set-display-name`). The join-dialog "Save"
    button also persists a typed name.
13. **desktop-task guard**: `report_tamper.py --task ...` against a non-operator
    mesh returns 409 (no charge for an undeliverable task); against an already-armed
    task returns 409.

### Windows desktop collar (commit 2ac3bf9) — needs a Windows box
14. Build with `python build-win.py`; confirm `safeword.exe` lands in `C:\focuslock`
    after `self_install`.
15. **Release Forever with UAC declined**: the farewell **warns** the collar will
    return (not "you are free"); accepting the UAC prompt fully removes the
    scheduled tasks + `C:\focuslock` + the lock-screen policy.
16. `safeword.exe` double-click removes everything incl. the HKLM lock-screen policy.

## Deferred follow-ups (tracked in CHANGELOG)
- `report_tamper.py` escalation counter is still local (a rooted bunny can reset it
  to the $5 floor); a server-authoritative per-mesh tamper tier would be tamper-proof.
- The Collar `"pixel"` `node_id` fallback can still register a phantom vault row on a
  join-time race; needs reordering `joinMesh`'s Settings writes in ControlService.
- Endpoint-level regression tests for the new `set-display-name` / `desktop-task`
  guards (the core `join()` preservation is unit-tested; these need the live-server
  + RSA-signing harness).
