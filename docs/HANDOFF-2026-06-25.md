# Handoff — 2026-06-25 (branch `feat/ecosystem-review-2026-05-25`, UNCOMMITTED)

Big multi-part effort: make the homelab optional + direct device-to-device primary,
add onboarding for both apps, add email onboarding (4 kinds) with strict Lion↔Bunny
email isolation, and embed Tor for cross-network. Plus a full waydroid QA.

## Toolchain (installed this session — survives in shell profiles)
- `ANDROID_SDK=/home/<user>/android-sdk`, `JAVA_HOME=/usr/lib/jvm/java-25-openjdk` (javac 25; build needs ≥17).
  Written to `~/.config/fish/conf.d/focuslock-android.fish` + `~/.config/focuslock-android.env.sh`.
- **Build an app:** `ANDROID_SDK=/home/<user>/android-sdk JAVA_HOME=/usr/lib/jvm/java-25-openjdk bash android/<slave|companion|controller>/build.sh`
- **Run py tests:** `SP="$(pwd)/.venv/lib/python3.14/site-packages"; PYTHONPATH="$SP:$(pwd):$(pwd)/shared" python3 -m pytest tests/<file> -q`
  (full suite errors under py3.14 — run subsets. `TestDesktopPairCreateAuthFunctional` has a pre-existing py3.14 "Bad file descriptor" error, unrelated.)
- **waydroid:** Fedora 44, binderfs is builtin. Container FREEZES when no UI shown (adb port closes) — keep `waydroid show-full-ui` running. adb needs `persist.adb.secure 0` + wireless-debugging on, or push the host key. `waydroid app install` silently no-ops → use `adb install`. The Collar resists uninstall (anti-tamper, by design) and leaves an orphaned lock-task; to reset: `sudo systemctl stop waydroid-container && sudo rm -rf /var/lib/waydroid/data/* && sudo systemctl start waydroid-container`.

## DONE & verified

| Task | What | Verified |
|---|---|---|
| A1 | Direct-first multi-address failover (LAN→Tailscale→.onion→relay) in Lion's Share `api()`/`postDirectWithFailover`, sticky endpoint, opportunistic refresh, relay fallback. Collar advertises `addresses/tailscale_ip/onion/direct_port` in `/api/pair` + `/mesh/status` (`selfAddressFields()`). Desktop `focuslock_sync.py` reordered. | 40 sync tests; curl `/mesh/status` + `/api/pair` on-device |
| A2 | `homelabConfigured()`/`applyHomelabGating()` hide payment-email/body-check (Lion) + IMAP/payer (Bunny). | on-device (hidden) |
| A4 | On-device subscription ticker (Collar); photo→Lion review; evidence→inbox; manual payment. | builds |
| B1/B2 | Lion gold wizard + Bunny lavender welcome (independent of Collar ToS). | on-device, screenshots |
| C1 | `scripts/setup-waydroid-fedora.sh` (Fedora SDK+JDK+waydroid). | user ran it |
| **#9** | **Lion-email→Bunny vault LEAK fixed** (found by adversarial workflow; 5-layer: ORDER_KEYS, `_admin_order_to_vault_blob` denylist, mesh_apply_order no-op, `_apply_initial_mesh_config`→`set_payee`, `_resolve_imap_creds` drops vault, migration purges, signup.html). | 4 new isolation tests + 304 server tests |
| **#10** | Server email support: `account_email`+sha256(`account_pass`) in mesh create; per-mesh `evidence_email` in PaymentIdentity (`set_evidence_email`, signed `set-evidence-email` endpoint, `initial_config.evidence_email`); `send_evidence(mesh_id=)` prefers per-mesh. All Lion-only. | 304 server tests |
| **#11** | Lion onboarding email panels — 3 panels (account email+pw, payment IMAP, evidence email) at idx 3-5, **only on the relay/Advanced path** (user's choice); conditional nav; `finishWithMethod` extras → `onActivityResult` → extended `createMesh(...7 args)` seeds account + `initial_config{imap,evidence}` (server-only, never vault). | builds clean |
| **#12** | Bunny payer panel (idx 3) → stashes `payer_identity_text`+`payer_identity_pending` → companion poller `maybeSendPendingPayerIdentity()` deferred-sends once paired (Bunny-signed, never to Lion). | builds clean |

**Privacy model (verified by 3 adversarial auditors):** Lion emails ONLY in `LionOnboardingActivity`→server-only stores; Bunny payer ONLY in `BunnyWelcomeActivity`→server-only allowlist. The Bunny→Lion direction is clean (counts-only responses, no email in /messages//api/pair/Settings.Global).

## PENDING — #13 native Tor (the user's "2")
Spec + **full code-ready recipe** in `docs/TOR-ONION.md` (incl. ADD_ONION ED25519-V3 ClientAuthV3, offline ed25519→.onion derivation, the base32-vs-base64 key-encoding footgun, OnionControl.java same-package helper, SOCKS-via-HttpURLConnection caveat, build.sh edits for bcprov + `find`-based globs). tor-android 0.4.9.9.1 AAR + jtorctl 0.4.5.7 were downloaded to a session scratchpad (re-fetch — URLs in the doc). `build.sh` has a DEFAULT-OFF `FOCUSLOCK_TOR_AAR` hook; `maybeWakeBunny()` is a no-op stub; `.onion` candidate plumbing + `focus_lock_onion_addr` advertisement already exist. To do: bcprov dep, `OnionControl.java`, Collar+Lion `TorManager.java`, declare `org.torproject.jni.TorService` in both manifests. Build with compiler in the loop; validate on devices + Tor network.

## Also pending
- On-device waydroid QA of the new email-onboarding panels (needs waydroid data reset — see above).
- **Commit** — nothing committed. Suggested grouping: (a) A1/A2/A4 connectivity, (b) B1/B2 onboarding, (c) #9 leak fix (security), (d) #10–#12 email onboarding, (e) #13 Tor when done. End commit messages with the Co-Authored-By line.

See memory `homelab-optional-direct-first-2026-06-25` for the condensed version.
